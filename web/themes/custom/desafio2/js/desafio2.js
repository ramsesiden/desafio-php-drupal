/**
 * @file
 * desafio2 behaviors.
 */
(function (Drupal) {

    'use strict';

    Drupal.behaviors.desafio2 = {
        attach(context, settings) {

            console.log('It works!');

        }
    };

} (Drupal));
